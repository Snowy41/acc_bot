{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "strict": true,
    "jsx": "react-jsx",  // Ensure JSX is set to react-jsx
    "esModuleInterop": true
  },
 "include": [
    "src/**/*",  // Ensure all your files in src are included
    "src/components/**/*"  // Specifically include components folder if it's separate
  ]
}
